variable "github_token" {
  description = "The token used to authenticate the GitHub provider for it to be able to create secrets in repositories in SumoLogic-Incubator organization"
  type        = string
}
